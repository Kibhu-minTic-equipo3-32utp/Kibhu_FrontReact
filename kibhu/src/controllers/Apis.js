const server = 'http://localhost:5000'

const apiClient = `${server}/api/clients`;

const apiSingleClient = `${apiClient}/identification`;

const apiSuplier = `${server}/supliers`;

export {apiClient, apiSingleClient, apiSuplier};